{
  "name": "receipt-server",
  "version": "1.0.0",
  "description": "This is the nodejs server that gets connected to the printer",
  "main": "printer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "escpos-serialport": "^3.0.0-alpha.4",
    "escpos-custom": "^3.0.0-alpha.7",
    "node-emoji": "^1.10.0",
    "pg": "^8.3.3",
    "serialport": "^8.0.5",
    "socket.io-client": "^4.0.0"
  }
}
